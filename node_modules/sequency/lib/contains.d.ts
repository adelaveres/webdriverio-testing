import Sequence from "./Sequence";
/**
 * Returns `true` if the sequence contains the given `element`.
 *
 * @param {T} element
 * @returns {boolean}
 */
declare function contains<T>(this: Sequence<T>, element: T): boolean;
export default contains;
