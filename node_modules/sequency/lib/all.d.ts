import Sequence from "./Sequence";
/**
 * Returns `true` if all elements match the given `predicate`.
 *
 * @param {(T) => boolean} predicate
 * @returns {boolean}
 */
declare function all<T>(this: Sequence<T>, predicate: (T) => boolean): boolean;
export default all;
