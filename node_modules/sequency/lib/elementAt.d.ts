import Sequence from "./Sequence";
/**
 * Returns the element at position `index` (zero-based) or throws an error if `index`
 * is out of bounds.
 *
 * @param {number} index
 * @returns {T}
 */
declare function elementAt<T>(this: Sequence<T>, index: number): T;
export default elementAt;
