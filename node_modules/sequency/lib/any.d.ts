import Sequence from "./Sequence";
/**
 * Returns `true` if at least one element match the given `predicate`.
 *
 * @param {(T) => boolean} predicate
 * @returns {boolean}
 */
declare function any<T>(this: Sequence<T>, predicate?: (T) => boolean): boolean;
export default any;
