import Sequence from "./Sequence";
/**
 * Performs the given `action` (side-effect) for each element of the sequence.
 *
 * @param {(T) => void} action
 */
declare function forEach<T>(this: Sequence<T>, action: (T) => void): void;
export default forEach;
