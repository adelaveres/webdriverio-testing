import Sequence from "./Sequence";
/**
 * Returns the element at position `index` (zero-based) or `null` if `index`
 * is out of bounds.
 *
 * @param {number} index
 * @returns {T}
 */
declare function elementAtOrNull<T>(this: Sequence<T>, index: number): T | null;
export default elementAtOrNull;
